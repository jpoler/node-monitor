apiVersion: 1

providers:
- name: NodeExporter
  folder: NodeExporter
  type: file
  options:
    path: /tmp/node-exporter.json
- name: DCGMExporter
  folder: DCGMExporter
  type: file
  options:
    path: /tmp/dcgm-exporter.json
